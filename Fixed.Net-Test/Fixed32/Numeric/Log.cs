using Lwkit.Fixed;

namespace Test
{
    //[Test]
    //internal class TLog : BaseTest<Fixed32>
    //{
    //    private const int LOOP_TIMES = 100;
    //    private const int MIN_NUMBER = 0;
    //    private const int MAX_NUMBER = int.MaxValue;

    //    public override void Run()
    //    {
    //        // 
    //        for (int i = 0; i < LOOP_TIMES; i++)
    //        {
    //            var b1 = Random.Shared.Next(MIN_NUMBER, MAX_NUMBER);
    //            var b2 = Random.Shared.NextDouble() * b1;

    //            var f1 = new Fixed32(b1);
    //            var f2 = new Fixed32(b2);

    //            Assert(Fixed32.Log(f1),   Math.Log(b1));
    //            Assert(Fixed32.Log(f2),   Math.Log(b2));
    //            Assert(Fixed32.Log2(f1),  Math.Log2(b1));
    //            Assert(Fixed32.Log2(f2),  Math.Log2(b2));
    //            Assert(Fixed32.Log10(f1), Math.Log10(b1));
    //            Assert(Fixed32.Log10(f2), Math.Log10(b2));
    //        }
    //    }
    //}
}
